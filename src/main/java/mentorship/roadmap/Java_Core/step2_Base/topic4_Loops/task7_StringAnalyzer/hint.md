# Подсказка

- Используйте `Scanner` + `nextLine()`.
- Можно пройтись по символам через `for (char ch : str.toCharArray())`.
- Методы `Character.isDigit(ch)`, `Character.isLetter(ch)`, `Character.isWhitespace(ch)` помогут с анализом.
