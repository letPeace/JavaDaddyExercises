# Подсказка

- `Transport` должен быть `abstract`, `move()` тоже
- В `Engine` увеличивайте `engineCount` в конструкторе
- Используйте `super(model)` в `Car`
- Для вывода: `engine.getHorsePower()` и `engine.getEngineCount()`
